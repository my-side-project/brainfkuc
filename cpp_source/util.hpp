#include <vector>

using namespace std;

namespace util {
    int add(const int val, const int to_add);

    int subtract(const int val, const int to_subtract);

    string clean_program(const string &program);
}
