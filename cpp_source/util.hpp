#include <vector>

using namespace std;

namespace util {
    int add(const int val, const int to_add, const int offset);

    int subtract(const int val, const int to_subtract, const int offset);

    string clean_program(const string &program);
}
